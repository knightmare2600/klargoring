# Build output -- large binaries, regenerated by build/build-installer-initrd.sh,
# never committed (each build produces a fresh kernel/initrd pair).
output/
outpu/
*.img
vmlinuz
KERNEL_VERSION

# Claude Code's own local project settings, machine-specific.
.claude/

# Python
__pycache__/
*.pyc
